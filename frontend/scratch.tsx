import * as React from "react"
import { Slot } from "@radix-ui/react-slot" // Wait, I didn't install radix. I will not use Slot for now, just standard button props. Let me fix the code content before returning.
