<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_esg_challenge_tree" model="ir.ui.view">
        <field name="name">esg.challenge.tree</field>
        <field name="model">esg.challenge</field>
        <field name="arch" type="xml">
            <tree string="Challenges">
                <field name="reference"/>
                <field name="name"/>
                <field name="type"/>
                <field name="start_date"/>
                <field name="end_date"/>
                <field name="xp_reward"/>
                <field name="state" widget="badge" decoration-info="state == 'draft'" decoration-success="state == 'active'" decoration-muted="state == 'completed'"/>
            </tree>
        </field>
    </record>

    <record id="view_esg_challenge_form" model="ir.ui.view">
        <field name="name">esg.challenge.form</field>
        <field name="model">esg.challenge</field>
        <field name="arch" type="xml">
            <form string="Challenge">
                <header>
                    <button name="action_start" type="object" string="Start Challenge" class="oe_highlight" invisible="state != 'draft'"/>
                    <button name="action_complete" type="object" string="End Challenge" class="oe_highlight" invisible="state != 'active'"/>
                    <field name="state" widget="statusbar" statusbar_visible="draft,active,completed"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1>
                            <field name="reference" readonly="1"/>
                        </h1>
                        <h2>
                            <field name="name" placeholder="Challenge Name"/>
                        </h2>
                    </div>
                    <group>
                        <group>
                            <field name="type"/>
                            <field name="start_date"/>
                            <field name="end_date"/>
                        </group>
                        <group>
                            <field name="xp_reward"/>
                            <field name="badge_reward_id"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Participants" name="participants">
                            <field name="participation_ids">
                                <tree editable="bottom">
                                    <field name="user_id"/>
                                    <field name="progress" widget="progressbar"/>
                                    <field name="state" widget="badge" decoration-success="state == 'completed'"/>
                                </tree>
                            </field>
                        </page>
                        <page string="Description" name="description">
                            <field name="description" placeholder="Challenge details..."/>
                        </page>
                    </notebook>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids"/>
                    <field name="activity_ids"/>
                    <field name="message_ids"/>
                </div>
            </form>
        </field>
    </record>

    <record id="action_esg_challenge" model="ir.actions.act_window">
        <field name="name">Challenges</field>
        <field name="res_model">esg.challenge</field>
        <field name="view_mode">tree,form</field>
    </record>
</odoo>
