<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_esg_compliance_issue_tree" model="ir.ui.view">
        <field name="name">esg.compliance.issue.tree</field>
        <field name="model">esg.compliance.issue</field>
        <field name="arch" type="xml">
            <tree string="Compliance Issues">
                <field name="reference"/>
                <field name="name"/>
                <field name="severity" widget="badge" decoration-danger="severity in ('high', 'critical')" decoration-warning="severity == 'medium'"/>
                <field name="department_id"/>
                <field name="assigned_to"/>
                <field name="state" widget="badge" decoration-info="state == 'in_progress'" decoration-success="state == 'resolved'" decoration-danger="state == 'escalated'"/>
            </tree>
        </field>
    </record>

    <record id="view_esg_compliance_issue_form" model="ir.ui.view">
        <field name="name">esg.compliance.issue.form</field>
        <field name="model">esg.compliance.issue</field>
        <field name="arch" type="xml">
            <form string="Compliance Issue">
                <header>
                    <button name="action_start" type="object" string="Start Investigation" class="oe_highlight" invisible="state != 'open'"/>
                    <button name="action_escalate" type="object" string="Escalate" invisible="state in ('resolved', 'escalated')"/>
                    <button name="action_resolve" type="object" string="Resolve" class="oe_highlight" invisible="state == 'resolved'"/>
                    <field name="state" widget="statusbar" statusbar_visible="open,in_progress,resolved"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1>
                            <field name="reference" readonly="1"/>
                        </h1>
                        <h2>
                            <field name="name" placeholder="Issue Title"/>
                        </h2>
                    </div>
                    <group>
                        <group>
                            <field name="audit_id"/>
                            <field name="department_id"/>
                        </group>
                        <group>
                            <field name="severity"/>
                            <field name="assigned_to"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Description" name="description">
                            <field name="description" placeholder="Describe the compliance issue in detail..."/>
                        </page>
                        <page string="Resolution" name="resolution">
                            <field name="resolution" placeholder="Corrective actions taken..."/>
                        </page>
                    </notebook>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids"/>
                    <field name="activity_ids"/>
                    <field name="message_ids"/>
                </div>
            </form>
        </field>
    </record>

    <record id="action_esg_compliance_issue" model="ir.actions.act_window">
        <field name="name">Compliance Issues</field>
        <field name="res_model">esg.compliance.issue</field>
        <field name="view_mode">tree,form</field>
    </record>
</odoo>
