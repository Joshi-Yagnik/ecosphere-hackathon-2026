<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_esg_audit_tree" model="ir.ui.view">
        <field name="name">esg.audit.tree</field>
        <field name="model">esg.audit</field>
        <field name="arch" type="xml">
            <tree string="Audits">
                <field name="reference"/>
                <field name="name"/>
                <field name="type"/>
                <field name="department_id"/>
                <field name="scheduled_date"/>
                <field name="score"/>
                <field name="state" widget="badge" decoration-info="state == 'in_progress'" decoration-success="state == 'completed'" decoration-danger="state == 'cancelled'"/>
            </tree>
        </field>
    </record>

    <record id="view_esg_audit_form" model="ir.ui.view">
        <field name="name">esg.audit.form</field>
        <field name="model">esg.audit</field>
        <field name="arch" type="xml">
            <form string="Audit">
                <header>
                    <button name="action_start" type="object" string="Start Audit" class="oe_highlight" invisible="state != 'scheduled'"/>
                    <button name="action_complete" type="object" string="Mark Completed" class="oe_highlight" invisible="state != 'in_progress'"/>
                    <button name="action_cancel" type="object" string="Cancel" invisible="state in ('completed', 'cancelled')"/>
                    <field name="state" widget="statusbar" statusbar_visible="scheduled,in_progress,completed"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1>
                            <field name="reference" readonly="1"/>
                        </h1>
                        <h2>
                            <field name="name" placeholder="Audit Title"/>
                        </h2>
                    </div>
                    <group>
                        <group>
                            <field name="type"/>
                            <field name="department_id" invisible="type == 'certification'"/>
                            <field name="auditor"/>
                        </group>
                        <group>
                            <field name="scheduled_date"/>
                            <field name="completed_date" readonly="1"/>
                            <field name="score"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Findings / Issues" name="issues">
                            <field name="issue_ids" context="{'default_audit_id': id, 'default_department_id': department_id}">
                                <tree editable="bottom">
                                    <field name="name"/>
                                    <field name="severity"/>
                                    <field name="assigned_to"/>
                                    <field name="state"/>
                                </tree>
                            </field>
                        </page>
                        <page string="Notes" name="notes">
                            <field name="notes" placeholder="Audit summary..."/>
                        </page>
                    </notebook>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids"/>
                    <field name="activity_ids"/>
                    <field name="message_ids"/>
                </div>
            </form>
        </field>
    </record>

    <record id="action_esg_audit" model="ir.actions.act_window">
        <field name="name">Audits</field>
        <field name="res_model">esg.audit</field>
        <field name="view_mode">tree,form</field>
    </record>
</odoo>
